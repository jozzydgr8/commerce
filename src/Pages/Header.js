import { Carousel } from "./Carousel"

export const Header = ()=>{
    return (
        <main>
            <h1 className="ubuntu">
                the trade mock up store
            </h1>
            <em>everything slick and cool </em>
            <div>
                <Carousel/>
            </div>
        </main>
    )
}